name: Pull Request title

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

permissions:
  pull-requests: read # required by action-semantic-pull-request to read the PR title via the GitHub API

jobs:
  pull-request:
    uses: nozomiishii/workflows/.github/workflows/pull-request.yaml@2b35b9debc19be8305aea68aa7c26d815ccd4c2d # v3.0.1
