name: Pull Request title

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

permissions: {}

jobs:
  pull-request:
    permissions:
      pull-requests: write # write: format revert PR titles, read: validate PR titles
    uses: nozomiishii/workflows/.github/workflows/pull-request.yaml@8712ffb685156b3b6f6ab6e510be6e087dae0f6c # v3.3.2
