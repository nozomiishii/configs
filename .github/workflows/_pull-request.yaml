name: Pull Request title

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

permissions:
  pull-requests: read # required by action-semantic-pull-request to read the PR title via the GitHub API

jobs:
  pull-request:
    uses: nozomiishii/workflows/.github/workflows/pull-request.yaml@91314aeeecdc7243ffe8ed3163eefebec2572f77 # v3.1.1
