name: Pull Request title

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

permissions:
  pull-requests: read # required by action-semantic-pull-request to read the PR title via the GitHub API

jobs:
  pull-request:
    uses: nozomiishii/workflows/.github/workflows/pull-request.yaml@ad828fad1cd5ef889d3dc52b669b4e409069f61f # v3.1.0
