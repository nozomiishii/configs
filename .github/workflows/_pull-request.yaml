name: Pull Request title

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

permissions: {}

jobs:
  pull-request:
    permissions:
      pull-requests: write # write: format revert PR titles, read: validate PR titles
    uses: nozomiishii/workflows/.github/workflows/pull-request.yaml@802b2a149c85be75ba2e020afcadefecfed3d82c # v3.3.0
