name: Secret scan

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  secret-scan:
    uses: nozomiishii/workflows/.github/workflows/secret-scan.yaml@91314aeeecdc7243ffe8ed3163eefebec2572f77 # v3.1.1
